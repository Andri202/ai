# Setup the agent

create the folder project:
	touch ai-qa-agent
    mkdir ai-qa-agent
	cd ai-qa-agent

create virtual env:
	python -m venv venv
	source venv/bin/activate

install the requirements and set up the .env
    pip install -r requirements.txt
    create a .env file based on .env.example


happy testing!!!

